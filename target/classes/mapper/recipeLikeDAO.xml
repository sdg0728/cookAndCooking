<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.proj.beans.like.RecipeLikeDAO">

	<select id="select" resultType="com.proj.beans.like.RecipeLikeDTO">
		SELECT * FROM r_like
	</select>
	
	<select id="selectByUid" resultType="com.proj.beans.like.RecipeLikeDTO">
		SELECT * FROM r_like WHERE user_id = {user_id}
	</select>

</mapper>